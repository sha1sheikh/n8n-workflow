# Client: <CLIENT NAME>

Copy this `_TEMPLATE` folder to `clients/<client-name>/` when you start a new
client, and fill in the sections below. Keep the delivered `workflow.json`(s)
in this folder so you always have a record of exactly what you shipped.

```
clients/<client-name>/
├── README.md          ← this file (the brief + record)
└── workflows/
    └── <workflow>.json ← the customized version you delivered
```

## Contact
- Name / company:
- Email:
- n8n instance: (their cloud URL, self-hosted, or "hosted by me")

## What they wanted
- Problem being solved:
- Trigger (what starts it):
- Actions (what it should do):
- Systems involved (Slack, Sheets, CRM, email, etc.):

## What I delivered
- Template(s) used:
- Customizations made:
- Date delivered:
- Webhook / production URLs (no secrets — references only):

## Credentials the client provided (names only — NEVER store secrets here)
- [ ] e.g. Google Sheets (OAuth2)
- [ ] e.g. Slack (OAuth2)

## Commercials
- Package / tier:
- Build fee:
- Monthly retainer:
- Renewal / review date:

## Notes & quirks
-
